describe('DBappEngine', function() {

  beforeEach(module('ecopos.common'));

  it('should ...', inject(function(DBappEngine) {

	//expect(DBappEngine.doSomething()).toEqual('something');

  }));

});